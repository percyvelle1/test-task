name: devops-test
services:
  app:
    build: ./app
    env_file: .env
    restart: unless-stopped
    networks: [web]

  nginx:
    build: ./nginx
    depends_on:
      - app
    ports:
      - "80:80"
    volumes:
      - nginx_logs:/var/log/nginx
    restart: unless-stopped
    networks: [web]

volumes:
  nginx_logs:

networks:
  web:
    driver: bridge
